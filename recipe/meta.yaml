{% set name = "quadpy" %}
{% set version = "0.16.6" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/quadpy-{{ version }}.tar.gz
  sha256: a665f1d3f9fe8e1444a92a6d2cac288d423eed49418dbb9e15457e71fee4fa07

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >3.5
  run:
    - importlib-metadata
    - ndim
    - numpy
    - orthopy <0.9,>=0.8.3
    - python >3.5
    - scipy
    - sympy

test:
  imports:
    - quadpy
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/nschloe/quadpy
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  summary: Numerical integration (quadrature, cubature) in Python
  description: |
    Your one-stop shop for numerical integration in Python.
  doc_url: https://github.com/nschloe/quadpy
  dev_url: https://github.com/nschloe/quadpy

extra:
  recipe-maintainers:
    - jan-janssen
